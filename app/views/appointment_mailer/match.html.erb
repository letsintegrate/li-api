<%- %i(de ar en).each do |locale| %>
  <%- I18n.with_locale(locale) do %>
    <%= render partial: 'match_general', locals: { appointment: @appointment } %>
    <%= render(partial: "locations/#{@location.slug.underscore}") rescue '' %>
  <% end %>
<% end %>
